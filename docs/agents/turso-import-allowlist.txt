# Transitional allowlist for `vox ci turso-import-guard`
#
# Built-in prefixes: `crates/vox-db/`, `crates/vox-package/`, `crates/vox-compiler/`.
# Prefer typed `VoxDb` APIs from application crates; see codex-turso-allowlist.md.
crates/vox-corpus/
crates/vox-cli/src/commands/db/
crates/vox-cli/src/commands/review/coderabbit/
crates/vox-workflow-runtime/tests/
crates/vox-ludus/
crates/vox-populi/src/transport/store/

# vox-codegen emits `turso::params![]` literals into generated Rust source.
# False-positive of the text-based guard regex; the crate does not link turso.
# A future tightening of the guard (skip code-string contexts) could remove
# these entries.
crates/vox-codegen/src/codegen_rust/

# vox-gamify/src/db/disputes.rs — one direct turso::params! call site.
# Transitional: should migrate to a typed VoxDb method in store/ops_ludus/.
crates/vox-gamify/src/db/

# vox-plugin-populi-mesh — direct turso usage in mesh transport store.
# Mirrors the existing crates/vox-populi/src/transport/store/ pattern (likely
# a renamed/refactored copy). Transitional until plugin-host transport is
# unified.
crates/vox-plugin-populi-mesh/src/transport/store/

# vox-secrets: tier-A relational owner per contracts/db/data-storage-policy.v1.yaml.
# Clavis vault uses a separate libSQL DB file (.vox/clavis_vault.db) for blast-radius
# isolation from user-data Codex; intentional and documented in ADR-004.
crates/vox-secrets/
