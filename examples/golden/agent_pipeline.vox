// ---
// title: "Agent Pipeline Example"
// description: "Structured task data as ADT plus actor message handler (core-parser form; actor block state is not in grammar yet)."
// syntax_version: "0.8"
// status: golden
// category: example
// constructs: [type, actor, on, fn, str]
// last_validated: 2026-03-26
// training_eligible: true
// training_weight: 1.0
// ---
// @training_prompt: Model task payloads as an ADT and handle them in an actor `on` handler.
// ANCHOR: display
type TaskMessage =
    | Msg(id: int, payload: str)

fn data_agent_ready() -> str {
    ret "Ready"
}

actor WorkerAgent {
    on HandleTask(id: int, payload: str) -> str {
        ret "Task " + str(id) + " done"
    }
}
// ANCHOR_END: display
