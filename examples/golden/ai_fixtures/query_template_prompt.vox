// ---
// title: "AI fixture — query template prompt"
// syntax_version: "0.5.0"
// status: golden
// category: example
// training_eligible: true
// ---

@prompt(stage = Planner, schema = PlanBlob, redact = [api_key])
@uses(net)
fn plan_with_prompt(input: str) to str {
    return input
}

@test
fn test_plan_with_prompt_fixture() to Unit {
    let out = plan_with_prompt("draft implementation plan")
    assert(len(out) > 0)
}
