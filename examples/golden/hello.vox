// ---
// title: "Hello Example"
// description: "Minimal Vox function: typed parameter, string return, and the ret keyword."
// syntax_version: "0.8"
// status: golden
// category: example
// constructs: [fn, ret, str]
// last_validated: 2026-03-24
// training_eligible: true
// training_weight: 1.0
// ---
// @training_prompt: Write a minimal Vox hello-world function
fn hello(name: str) to str {
    ret "Hello " + name + "!"
}
