@endpoint(kind: query) fn get_count() to int { return 42 }

component C() {
    state label: str = "ready"
    view: column() {
        heading(level=1) { label }
        button(on_click={label = "clicked"}) { "Fetch" }
    }
}
