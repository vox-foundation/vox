# GPU specifications and training preset configurations for Vox Mens.
#
# SSOT for:
#   - Cloud cost estimation: TimeEstimator scales training time from TFLOPS ratios
#   - Local training: auto-preset selection based on available VRAM
#   - Cloud preset injection: resolver reads presets and passes them as container env
#
# Priority: measured DB profiles > TFLOPS-scaled heuristic > conservative fallback.
#
# Sources: NVIDIA product datasheets and official whitepapers.
# Add new GPUs without recompiling — loaded at runtime by TimeEstimator.
#
# SSOT doc: docs/src/architecture/mens-cloud-gpu-ssot.md

gpus:
  # ── Consumer GPUs (local training) ─────────────────────────────────────────

  # RTX 4080 Super — primary local training GPU. First-class default. SSOT.
  "rtx 4080 super":
    fp16_tflops: 52.2
    vram_mb: 16384

  "rtx 4080":
    fp16_tflops: 48.7
    vram_mb: 16384

  "rtx 4090":
    fp16_tflops: 82.6
    vram_mb: 24576

  "rtx 5090":
    fp16_tflops: 105.0
    vram_mb: 32768

  "rtx 5080":
    fp16_tflops: 75.0
    vram_mb: 16384

  "rtx 3090":
    fp16_tflops: 35.6
    vram_mb: 24576

  "rtx 3090 ti":
    fp16_tflops: 40.0
    vram_mb: 24576

  "rtx 4070 ti super":
    fp16_tflops: 40.0
    vram_mb: 16384

  # ── Datacenter — Ampere ─────────────────────────────────────────────────────

  "a100-sxm4-80gb":
    fp16_tflops: 77.97
    vram_mb: 81920

  "a100 sxm 80g":
    fp16_tflops: 77.97
    vram_mb: 81920

  "a100-sxm4-40gb":
    fp16_tflops: 77.97
    vram_mb: 40960

  "a100 pcie 80gb":
    fp16_tflops: 77.97
    vram_mb: 81920

  "a100 pcie 40gb":
    fp16_tflops: 77.97
    vram_mb: 40960

  "a10g":
    fp16_tflops: 31.2
    vram_mb: 24576

  "a10":
    fp16_tflops: 31.2
    vram_mb: 24576

  # ── Datacenter — Ada Lovelace ───────────────────────────────────────────────

  "l4":
    fp16_tflops: 30.3
    vram_mb: 24576

  "l40":
    fp16_tflops: 90.5
    vram_mb: 49152

  "l40s":
    fp16_tflops: 91.6
    vram_mb: 49152

  "a6000":
    fp16_tflops: 38.7
    vram_mb: 49152

  "a6000 ada":
    fp16_tflops: 91.1
    vram_mb: 49152

  # ── Datacenter — Hopper ────────────────────────────────────────────────────

  "h100 sxm":
    fp16_tflops: 267.0
    vram_mb: 81920

  "h100 pcie":
    fp16_tflops: 204.9
    vram_mb: 81920

  "h100 nvl":
    fp16_tflops: 267.0
    vram_mb: 188416

  # ── Datacenter — Hopper+ ───────────────────────────────────────────────────

  "h200":
    fp16_tflops: 267.0
    vram_mb: 147456

  "gh200":
    fp16_tflops: 267.0
    vram_mb: 98304

  # ── Datacenter — Blackwell ─────────────────────────────────────────────────

  "b200":
    fp16_tflops: 450.0
    vram_mb: 196608

  "b100":
    fp16_tflops: 360.0
    vram_mb: 188416

  # ── Datacenter — Turing / Volta (older; common on Vast.ai) ─────────────────

  "v100-sxm2-32gb":
    fp16_tflops: 28.3
    vram_mb: 32768

  "v100-sxm2-16gb":
    fp16_tflops: 28.3
    vram_mb: 16384

  "v100 pcie 16gb":
    fp16_tflops: 28.3
    vram_mb: 16384

  "t4":
    fp16_tflops: 8.1
    vram_mb: 16384

  # ── Consumer — Maxwell / Pascal (avoid for training) ──────────────────────

  "rtx 2080 ti":
    fp16_tflops: 13.4
    vram_mb: 11264

  "rtx 2080 super":
    fp16_tflops: 11.1
    vram_mb: 8192

# ── Training preset configurations ──────────────────────────────────────────
#
# SSOT for both local and cloud training preset selection.
# `vox mens train --preset auto` reads these to pick the best config for
# the available VRAM. Cloud dispatch reads the same to populate container env.
#
# Fields:
#   seq_len:     Training sequence length in tokens
#   batch_size:  Micro-batch size per step
#   grad_accum:  Gradient accumulation steps (effective batch = batch × grad_accum)
#   lr:          Learning rate
#   max_vram_mb: This preset fits in this many MB of VRAM (upper bound for selection)
#
# Selection rule: pick the preset with the largest max_vram_mb that still fits.

presets:
  tiny:
    seq_len: 256
    batch_size: 1
    grad_accum: 4
    lr: 5.0e-5
    max_vram_mb: 8192

  safe:
    seq_len: 384
    batch_size: 1
    grad_accum: 2
    lr: 3.0e-5
    max_vram_mb: 10240

  # Matches 12GB cards (RTX 3060, RTX 4070)
  prosumer_12g:
    seq_len: 512
    batch_size: 1
    grad_accum: 1
    lr: 2.0e-5
    max_vram_mb: 12288

  # Matches 16GB cards — RTX 4080 / 4080 Super / 4070 Ti Super (primary dev GPU)
  prosumer_16g:
    seq_len: 1024
    batch_size: 1
    grad_accum: 4
    lr: 2.0e-5
    max_vram_mb: 16384

  # Matches 24GB cards — RTX 3090 / 4090 / A10
  prosumer_24g:
    seq_len: 768
    batch_size: 2
    grad_accum: 1
    lr: 1.5e-5
    max_vram_mb: 24576

  # Matches NVIDIA A6000 (48GB), L40, L40S
  a6000:
    seq_len: 1024
    batch_size: 4
    grad_accum: 1
    lr: 1.0e-5
    max_vram_mb: 49152

  # Matches A100 40/80GB, H100 PCIe
  a100:
    seq_len: 2048
    batch_size: 8
    grad_accum: 1
    lr: 8.0e-6
    max_vram_mb: 81920

  # Matches H100 SXM, H200, B100, B200 (highest tier)
  h100:
    seq_len: 4096
    batch_size: 16
    grad_accum: 1
    lr: 5.0e-6
    max_vram_mb: 999999

# Training base candidates per capability tag (referenced by domain-profiles base.model).
# floor_mb = approx QLoRA VRAM floor on the standard preset. Resolver picks the largest
# variant whose floor_mb <= detected VRAM. methods = train methods this base supports.
# Keys match domain-profiles.yaml base.model values exactly.
train_bases:
  # Qwen3 dense ladder — revision-pinned. NOTE: every Qwen3 @<sha> below is a
  # real, pinned HF commit hash (verified by the vox-populi integration test
  # gpu_specs_agentic_default_qwen3_hf_ids_have_real_sha_format); the legacy
  # Qwen2.5-Coder rungs are intentionally left unpinned (floating to `main`).
  # Do not add a Qwen3 rung with an unpinned/placeholder revision —
  # ensure_not_placeholder() rejects it at train/dispatch time, but that
  # guard runs late; keep entries pinned up front.
  qwen3_code:
    - { hf_id: "Qwen/Qwen3-0.6B@c1899de289a04d12100db370d81485cdf75e47ca", floor_mb: 2000,  methods: [qlora] }   # CPU / ≤8 GB dev
    - { hf_id: "Qwen/Qwen3-8B@b968826d9c46dd6066d109eabc6255188de91218",   floor_mb: 12000, methods: [qlora] }   # 16 GB tier
    - { hf_id: "Qwen/Qwen3-14B@40c069824f4251a91eefaf281ebe4c544efd3e18",  floor_mb: 20000, methods: [qlora] }   # 24 GB tier (QLoRA)
    - { hf_id: "Qwen/Qwen3-14B@40c069824f4251a91eefaf281ebe4c544efd3e18",  floor_mb: 44000, methods: [lora] }    # 48 GB tier (un-quantized; beats 24g entry)
    - { hf_id: "Qwen/Qwen3-32B@9216db5781bf21249d130ec9da846c4624c16137",  floor_mb: 60000, methods: [qlora] }   # 96 GB tier

  # Alias tags updated to include Qwen3 rungs alongside existing Qwen2.5 bases.
  # Resolver picks largest fitting rung — at ≥12GB a Qwen3 rung wins.
  small_code_default:  # intentionally caps at 8B; strong_code_default escalates to larger rungs
    - { hf_id: "Qwen/Qwen2.5-Coder-3B-Instruct",         floor_mb: 6000,  methods: [qlora, full_sft] }
    - { hf_id: "Qwen/Qwen3-8B@b968826d9c46dd6066d109eabc6255188de91218",     floor_mb: 12000, methods: [qlora] }
  strong_code_default:
    - { hf_id: "Qwen/Qwen2.5-Coder-7B-Instruct",         floor_mb: 11000, methods: [qlora, dpo, orpo] }
    # USER DECISION (Qwen3 everywhere): the Qwen3-8B rung (floor 12000) outranks the
    # Qwen2.5-Coder-7B rung (floor 11000) so a 16 GB box resolves to Qwen3-8B.
    - { hf_id: "Qwen/Qwen3-8B@b968826d9c46dd6066d109eabc6255188de91218",     floor_mb: 12000, methods: [qlora] }
    - { hf_id: "Qwen/Qwen3-14B@40c069824f4251a91eefaf281ebe4c544efd3e18",    floor_mb: 20000, methods: [qlora] }
    - { hf_id: "Qwen/Qwen3-14B@40c069824f4251a91eefaf281ebe4c544efd3e18",    floor_mb: 44000, methods: [lora] }
    - { hf_id: "Qwen/Qwen3-32B@9216db5781bf21249d130ec9da846c4624c16137",    floor_mb: 60000, methods: [qlora] }
    # Apple Silicon 128 GB tier (usable ~116 GiB after the GUI reserve): 32B
    # un-quantized LoRA. floor_mb is a provisional estimate — 64 GB of BF16
    # weights plus optimizer/activation headroom — pending a measured peak
    # (docs/superpowers/plans/2026-09-05-mens-mac-training-enablement.md Task 4).
    - { hf_id: "Qwen/Qwen3-32B@9216db5781bf21249d130ec9da846c4624c16137",    floor_mb: 100000, methods: [lora] }
  agentic_default:
    - { hf_id: "Qwen/Qwen2.5-Coder-7B-Instruct",         floor_mb: 11000, methods: [qlora, dpo] }
    # USER DECISION (Qwen3 everywhere): the Qwen3-8B rung (floor 12000) outranks the
    # Qwen2.5-Coder-7B rung (floor 11000) so a 16 GB box resolves to Qwen3-8B.
    - { hf_id: "Qwen/Qwen3-8B@b968826d9c46dd6066d109eabc6255188de91218",     floor_mb: 12000, methods: [qlora] }
    - { hf_id: "Qwen/Qwen3-14B@40c069824f4251a91eefaf281ebe4c544efd3e18",    floor_mb: 20000, methods: [qlora] }
    - { hf_id: "Qwen/Qwen3-14B@40c069824f4251a91eefaf281ebe4c544efd3e18",    floor_mb: 44000, methods: [lora] }
    - { hf_id: "Qwen/Qwen3-32B@9216db5781bf21249d130ec9da846c4624c16137",    floor_mb: 60000, methods: [qlora] }
    # Apple Silicon 128 GB tier — see strong_code_default above for the note.
    - { hf_id: "Qwen/Qwen3-32B@9216db5781bf21249d130ec9da846c4624c16137",    floor_mb: 100000, methods: [lora] }

# TODO: B6.0 vLLM compat spike
# B6.0 requires standing up a real vLLM instance with Qwen3 + a trained QLoRA adapter.
# This is a PROCEDURE step requiring human action and hardware — it cannot be automated.
#
# Steps (pending human execution):
#   1. Stand up a vLLM instance: `vllm serve <base_model> --enable-lora --max-lora-rank 64`
#   2. Load a trained adapter via the /v1/models API or --lora-modules flag
#   3. Send a test request with `model=<adapter_name>` and verify the adapter is applied
#   4. Validate guided_json decoding works end-to-end (schema-constrained output)
#   5. Confirm multi-LoRA slot exhaustion behavior (what happens when max_loaded is exceeded)
#   6. Record measured latency and throughput vs. base model for the gate metric
#
# Pre-requisites:
#   - At least one trained QLoRA adapter from B2+ pipeline stages
#   - vLLM >= 0.4.0 with LoRA support enabled
#   - GPU with sufficient VRAM for base model + adapter overhead
#   - VOX_MENS_VLLM_BASE_URL env var set to the running vLLM endpoint
#
# Once completed, update this comment with:
#   - vLLM version confirmed working
#   - Max simultaneous adapters tested
#   - Latency overhead of LoRA vs. base (expected: <5ms per token)
