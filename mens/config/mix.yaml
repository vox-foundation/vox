# Production mix for Mens QLoRA: primary train.jsonl + tool-trace supervision rows.
# Run from repo root: `vox mens corpus mix --config mens/config/mix.yaml`
# `vox schola train` auto-runs mix when this file exists and copies output to `--data-dir/train.jsonl`.

sources:
  - path: mens/data/train.jsonl
    weight: 2
    optional: true              # Generated by pipeline; absent on first-run is fine
  - path: mens/data/train_full_backup.jsonl
    weight: 2
  - path: mens/data/tool_traces.example.jsonl
    weight: 2
    record_format: tool_trace
  - path: mens/data/synthetic.jsonl
    weight: 2
  - path: mens/data/golden_pairs.jsonl
    weight: 4                   # Human-curated pairs: highest signal weight
  - path: mens/data/synthetic_search.jsonl
    weight: 2
  - path: mens/data/golden_validated.jsonl
    weight: 2
  - path: mens/data/autofeedback.jsonl
    weight: 1
    optional: true              # Self-improvement loop; absent until first dogfood run

output: target/dogfood/train_mixed.jsonl
