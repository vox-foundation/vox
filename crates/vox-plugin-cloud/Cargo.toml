[package]
name = "vox-plugin-cloud"
version = "0.1.0"
edition.workspace = true
publish = false
description = "CloudSync plugin stub: HF Hub / S3 model artifact sync (SP7 scaffold; actual extraction deferred)."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
vox-plugin-api = { workspace = true }
abi_stable = { workspace = true }

[lints]
workspace = true
