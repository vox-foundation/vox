{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FleetQuotaConfig",
  "description": "Configuration for per-tenant and per-fleet spend isolation and budget caps",
  "type": "object",
  "required": ["daily_budget_usd", "monthly_budget_usd"],
  "properties": {
    "daily_budget_usd": {
      "type": "number",
      "description": "Aggregate daily budget across all agents in the fleet",
      "minimum": 0
    },
    "monthly_budget_usd": {
      "type": "number",
      "description": "Aggregate monthly budget across all agents in the fleet",
      "minimum": 0
    },
    "tenant_daily_caps": {
      "type": "object",
      "description": "Per-tenant budget isolation. Key is tenant ID.",
      "additionalProperties": {
        "type": "number",
        "minimum": 0
      }
    },
    "model_pinning_enabled": {
      "type": "boolean",
      "default": true
    },
    "allowed_model_tiers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": ["local", "mid", "frontier"]
    }
  }
}
