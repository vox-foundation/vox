[package]
name = "vox-research-events"
description = "Typed SCIENTIA research event types, PreregistrationV1, and ResearchEventEmitter trait (L1 — pure data, no async runtime)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
chrono = { workspace = true }
workspace-hack = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
