# Minimal full-stack surface — v0.4
# Used as a golden input for `vox build` integration tests.

component Home() {
    view: <div>"home"</div>
}

routes {
    "/" to Home
}

@endpoint(kind: server) fn echo(x: str) to str {
    return x
}
