[package]
name = "vox-release-artifacts"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Shared archive/checksum helpers for vox ci release-build and vox compile packaging."

[dependencies]
anyhow = "1"
flate2 = "1"
sha2 = "0.10"
tar = "0.4"
zip = { version = "2", default-features = false, features = ["deflate"] }
