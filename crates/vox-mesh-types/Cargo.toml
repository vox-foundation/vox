[package]
name = "vox-mesh-types"
version = "0.2.0"
edition = "2021"
description = "Core types for the Vox Federated GPU Mesh"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
