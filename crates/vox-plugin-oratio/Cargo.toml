[package]
name = "vox-plugin-oratio"
version = "0.1.0"
edition.workspace = true
publish = false
description = "AudioCapture plugin stub: Oratio speech-to-code pipeline (SP7 scaffold; actual extraction deferred)."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
vox-plugin-api = { workspace = true }
abi_stable = { workspace = true }

[lints]
workspace = true
