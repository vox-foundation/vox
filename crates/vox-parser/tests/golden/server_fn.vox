# Server function demo — v0.3 brace syntax
# Shows how @server generates both API route + typed fetch wrapper

type Greeting =
    | Hello(message: str)

@server fn greet(name: str) to Greeting {
    ret Hello("Welcome, " + name + "!")
}

@server fn add(a: int, b: int) to int {
    ret a + b
}
