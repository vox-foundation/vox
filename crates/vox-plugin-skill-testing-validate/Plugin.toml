[plugin]
id = "skill-testing-validate"
name = "Vox Testing Validate Skill"
version = "0.1.0"
description = "Agent-facing skill for test validation diagnostics."
license = "Apache-2.0"

[plugin.host]
min-vox-version = "0.5.0"

[plugin.payload]
kind = "skill"
format-version = 1
skill-md = "testing.validate.skill.md"

[plugin.payload.tools]
exposes = ["vox_test_validate"]
