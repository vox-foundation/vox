[package]
name = "vox-orchestrator-core"
description = "Workspace boundary marker for the core router/dispatcher of vox-orchestrator (the `orchestrator/` subdir, ~11.5K LoC). Full extraction is blocked by 30+ `crate::` cross-cuts into sibling modules (planning, services, budget, scope, affinity, bulletin, config, locks, groups, types, topology, snapshot, oplog, context, events, a2a, monitor, qa, catalog, models, heartbeat, tool_receipt, privacy_router, judge_model, reconstruction, attention, summary, workspace, conflicts). The architectural intent is documented here; the actual code remains in vox-orchestrator until a broader L3 split is feasible."
version.workspace = true
edition.workspace = true

[dependencies]
workspace-hack = { workspace = true }

[lints]
workspace = true
