[package]
name = "vox-exec-grammar"
description = "AST parser and risk classifier for shell/Vox command invocations; backs exec-policy.v1.yaml enforcement."
version.workspace = true
edition.workspace = true
license.workspace = true

# Design reference (study only, not copied): warpdotdev/warp crates/command-signatures-v2 (AGPL-3.0-only).
# See ADR-026 and docs/src/architecture/warp-research-findings-2026.md.

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
workspace-hack = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
proptest = "1.10.0"
