[package]
name = "vox-findings-site"
description = "SCIENTIA Phase G — pure renderer for the canonical /findings/<trusty-uri> page: server-rendered HTML body, version history, Highwire meta tags for Google Scholar, and a retraction banner when applicable."
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }

[dev-dependencies]
