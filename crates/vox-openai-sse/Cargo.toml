[package]
name = "vox-openai-sse"
description = "OpenAI-compat SSE line reassembly and chat completion delta extraction (runtime + Ludus shared)."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde_json = { workspace = true }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

[lints]
workspace = true
