[package]
name = "vox-plugin-catalog"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SSOT catalog of all first-party Vox plugins and distribution bundles."

[dependencies]
serde = { workspace = true, features = ["derive"] }
toml = { workspace = true }
thiserror = { workspace = true }
workspace-hack = { workspace = true }

[build-dependencies]
serde = { workspace = true, features = ["derive"] }
toml = { workspace = true }

[dev-dependencies]
toml = { workspace = true }

[lints]
workspace = true
