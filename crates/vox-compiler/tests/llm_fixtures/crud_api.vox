@table type User {
    id: int
    name: str
    email: str
}

fn create_user(name: str, email: str) -> Result[User] {
    let u = User { id: 0, name: name, email: email }
    return db.User.insert(u)
}

fn get_user(id: int) -> Result[Option[User]] {
    return db.User.get(id)
}
