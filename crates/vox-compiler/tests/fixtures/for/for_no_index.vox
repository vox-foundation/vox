# for_no_index.vox — for loop without index variable: `for r in rows { … }`
# Should emit (r, _i) => since no explicit index was provided.
component NoIndex(rows: Array) {
    view: (
        <div class="list">
            for r in rows {
                <span>{r.name}</span>
            }
        </div>
    )
}
