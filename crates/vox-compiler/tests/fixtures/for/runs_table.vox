// runs_table — for-loop with explicit index, key prop, multi-child rows.
component RunsTable(rows: any) {
    view: panel(class="table") {
        for r, i in rows key=r.id {
            panel(class="row") {
                span() { r.id }
                span() { r.duration }
            }
        }
    }
}
