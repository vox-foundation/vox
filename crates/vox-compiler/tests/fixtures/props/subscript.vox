component Indexed(items: Array, i: int) {
    view: (
        <div>
            <span>{items[0]}</span>
            <span>{items[i]}</span>
            <span>{items[i + 1]}</span>
        </div>
    )
}
