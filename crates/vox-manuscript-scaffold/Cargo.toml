[package]
name = "vox-manuscript-scaffold"
description = "SCIENTIA Phase C — IMRaD manuscript scaffolder that fills only provenance-bound safe slots and leaves forbidden sections as TODO blocks (per the worthiness rubric)."
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }

[dev-dependencies]
