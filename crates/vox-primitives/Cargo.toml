[package]
name = "vox-primitives"
description = "Dependency-neutral id and backoff helpers shared across workspace crates."
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
workspace-hack = { version = "0.1", path = "../workspace-hack" }
