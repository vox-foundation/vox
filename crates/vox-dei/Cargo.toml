[package]
name = "vox-dei"
version = "0.1.0"
edition = "2024"
publish = false
description = "Workspace-excluded DeI snapshot; policy alignment compiles standalone."

[lib]
path = "src/lib.rs"

[dependencies]
vox-socrates-policy = { path = "../vox-socrates-policy" }
