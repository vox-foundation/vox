[package]
name = "vox-dei"
version = "0.1.0"
edition = "2024"
publish = false
description = "Minimal DeI staging crate; Socrates policy floors only until legacy modules are rewired."

[lib]
path = "src/lib.rs"

[dependencies]
tracing = { workspace = true }
vox-socrates-policy = { path = "../vox-socrates-policy" }
