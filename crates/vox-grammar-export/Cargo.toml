[package]
name = "vox-grammar-export"
version = "0.1.0"
edition = "2021"

[dependencies]
semver = { version = "1.0", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
