//! Skill manifest types — re-exported from `vox-plugin-host`.
//!
//! These types have moved to `vox_plugin_host::skill_manifest`.
//! New code should import from `vox_plugin_host` directly.

pub use vox_plugin_host::skill_manifest::{SkillCategory, SkillManifest, SkillPermission};
