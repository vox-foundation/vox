//! `vox stub-check` — TOESTUB + Codex integration (full implementation in `diagnostics::stub_check`).

pub use crate::commands::diagnostics::stub_check::{
    check_rationale_required, explain_diagnostic, list_diagnostics, run,
};
