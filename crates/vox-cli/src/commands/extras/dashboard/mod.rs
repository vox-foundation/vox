//! Dei Dashboard — HTTP server relocated to `vox-codex-api`.
//!
//! The full implementation has been moved to `vox_codex_api::dashboard`.
//! `vox-cli` now delegates `vox dash` to `vox_codex_api::run_dashboard()` directly.
//! This module is retained only as a stub to satisfy any residual re-export paths.
