[package]
name = "vox-scientia-social"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vox-publisher = { path = "../vox-publisher", default-features = false }
workspace-hack = { version = "0.1", path = "../workspace-hack" }
