[package]
name = "vox-orchestrator-cap-mint"
description = "Sealed-trait facade authorizing capability mints for vox-orchestrator-types. The only crate permitted to call the sealed mint hooks; downstream callers cannot construct capabilities directly."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vox-orchestrator-types = { workspace = true }

[lints]
workspace = true
