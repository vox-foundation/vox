# Rust
target/
.ci-runner-logs/
.sccache/
**/fuzz/target/
**/fuzz/artifacts/
**/fuzz/corpus/
target_*/
# Side `CARGO_TARGET_DIR=*` trees (mostly repo root); global `target-*/` also covers nested mistakes.
target-*/
.cargo-targets/
**/*.rs.bk
Cargo.lock

# Node
**/node_modules/
**/out/
node_modules/
dist/
dist1/
docs-astro/src/content/docs
# setup-content.mjs also symlinks this one from repo-root examples/, but a
# real 200-file copy was committed here at some point and has since drifted
# from the source of truth (13 files differ; the real examples/ tree was
# last touched 2026-08-30, this copy's last real edit was 2026-08-22). Once
# untracked, the symlink takes over and docs render the current examples.
docs-astro/src/examples
docs-astro/public/assets
docs-astro/public/.well-known
# Copied from docs/src/robots.txt by docs-astro/scripts/setup-content.mjs
docs-astro/public/robots.txt
.env

# Local TOESTUB / audit outputs (do not commit large JSON baselines)
/artifacts/
.tmp_audit/
# Compiled plugin binaries produced by full-runtime audit runs (build artifacts)
/.vox/audit/**/vox_plugin_*.dll
/.vox/audit/**/vox_plugin_*.so
/.vox/audit/**/vox_plugin_*.dylib

# CR-L8 corpus-feedback per-run events (P2.1c, ratified 2026-05-15).
# Per-day JSONL files written by `CorpusFeedbackJsonlSink` are measurement
# noise — not source-of-truth. The substantive quarterly report at
# contracts/reports/corpus-feedback/<quarter>.json IS the gated v1.0
# artifact and remains committed. See
# docs/src/architecture/v1-llm-target-implementation-plan-2026.md §1.3 P2.1c.
/contracts/reports/corpus-feedback-events/

# LLM-panel response cache (CR-L0/L1/L3/L4 panel runners). Content-addressed
# disk cache written by CachingPanelClient; regenerated on demand. The
# canonical evidence is the per-gate <UTC>.json artifact, not the cache.
# Per honest-completion-plan §3.x.
/contracts/reports/llm-panel-cache/

# GUI visual AI review — per-run dated JSON reports are CI upload artifacts only.
# Committed SSOT: cache.v1.json (+ ledger.jsonl when present). See docs/src/architecture/gui-visual-ai-review.md.
/contracts/reports/gui-visual-review/*.json
!/contracts/reports/gui-visual-review/cache.v1.json
!/contracts/reports/gui-visual-review/bundle-cache.v1.json

# Playwright HTML report (local e2e runs; never commit)
**/playwright-report/

# === SCRATCH / EPHEMERAL / LOGS ===
.tmp_metadata.json
scratch/*
!scratch/.gitkeep
test_output*.txt
bundle_*.txt
*.log


# Per-user Claude Code allowlist; contains machine-specific paths.
# Use `.claude/settings.json` (committed) for shared policy.
.claude/settings.local.json
.claude/settings.local.json.bak
.claude/worktrees/

# Local agent worktrees / clones (machine-specific; never commit)
.claire/
.clone/
.worktrees/

# act (nektos/act) secrets file — never commit; used by `vox ci pre-push --act`
.secrets

# Editor / IDE artifacts (keep shared `settings.json` for terminal PATH / CUDA)
.vscode/*
!.vscode/settings.json
.idea/
.cursor/plans/

# Python (local tooling under scripts/)
scripts/.venv/
.mypy_cache/
*.swp
*~

# CodeRabbit CLI worktrees and state (`vox review coderabbit`, `--features coderabbit`)
.coderabbit/

crates/*/pkg/
dist/app/node_modules/

# Populi / AI Models / Data
populi/runs/
populi/data/*.jsonl
.vox-research-data/
.vox/cache/
.vox/artifacts/
**/.vox/cache/
**/.vox/artifacts/
# Telemetry upload spool — runtime state written by vox-telemetry's
# upload queue; pending/ holds per-event JSON until upload succeeds.
# Not source-of-truth, regenerated on every run, should not be committed.
.vox/telemetry-upload-queue/
# Nested form, matching the **/ anchoring used for cache/ and artifacts/ above.
# Belt and braces only: a nested .vox is a BUG (see paths::repo_dot_vox_dir),
# and ignoring it must not become the way we stop noticing.
**/.vox/telemetry-upload-queue/
# Runtime memory stubs (regenerated locally; never commit)
/.vox/memory/
# Per-branch policy run-status overlay (Phase 1c): machine-specific, written by
# `vox ci`/code-audit/arch gates per branch. Regenerated on every run; never commit.
.vox/policy-status/
# Antigravity delegation worktrees (ephemeral, per-delegation)
/.vox/agy-worktrees/
**/.vox/policy-status/
# Crate-local `.vox/` state (orchestrator memory, etc.)
crates/*/.vox/
*.db-wal
*.db-shm
*.db
stub_check_results*.json
opencode.json
.opencode/
stub_check_results2.json

# CI link checker output wrongly committed at repo root (canonical output lives under docs-astro/)
/docs/lychee_report.json
**/check_errors.txt


# Jujutsu VCS
.jj/

# Local agent/tooling scratch under repo root (prefer OS temp for secrets)
/tmp/

# Scratch files
cargo-out.txt
test_all.txt
scratch*.txt
scratch*.log
*.bak
*.orig
crates/*/target/
crates/*/target_*/
# Quarantined Cargo target dirs (rename when deletes fail on locked binaries)
crates/*/target.stale-*/

# Root-level scratch/debug output (should never be committed)
/*.txt
/*.err
/*.bat
/*.py
/*.jsonl
/*_head.rs
/*_scratch*.rs
/dump.txt
/sql_dump.txt
/raw_err*.json
/check_err*.json
/clippy_output.json
/pm_errors.json
# GitHub Actions API exports (`gh run list --json`, `gh job list --json`)
/jobs.json
/runs.json

# Generated test fixture artifacts (heavy weights etc. re-created by build), but
# keep the small static HF `config.json` inputs that hf_load_config.rs reads.
crates/vox-populi/tests/fixtures/**
!crates/vox-populi/tests/fixtures/**/
!crates/vox-populi/tests/fixtures/**/*.json
docs/book/

# Large model and data artifacts
*.safetensors
*.jsonl
# Exception: test fixtures must be committable without `git add -f`.
# Scoped to **/tests/fixtures/** so target/ JSONL/log output stays ignored.
!**/tests/fixtures/**/*.jsonl
!**/tests/fixtures/**/*.log

# Corpus mix reports — machine-specific absolute paths; regenerated by `vox mens corpus mix`
mens/data/*.mix_report.json

# All training run directories are machine-local artifacts — never commit run artifacts.
# Named runs (agent_*, bg_*, v1, qwen35_*, etc.), logs, latest/, and all
# pipeline-generated artifacts (training_manifest.json, telemetry.jsonl,
# eval_results.json, gate_receipt.json, eval_local_report.json,
# benchmark_passatk.json, review_metrics.json, benchmark_gate_eval.json,
# training-preflight.json, checkpoint_state.json, etc.) are covered by this rule.
mens/runs/
# Quarantined mens scratch (e.g. renamed temp_stabilization trees)
/mens/.stale/

# Repo-root only: renamed `target` trees when deletes fail on locked binaries (e.g. Windows).
/target*.stale-*/

# Session artifacts — explicit patterns so *.txt negation doesn't hide these
/check*.txt
/check*.log
/cargo_check_output.txt
/check_output.txt
/check_errors.txt
/check_mens_errors*.txt
/cargo_errors*.txt
/train_out*.txt
/run*.txt
/build_log*.txt

# Root-level stale build/debug artifacts (never commit)
/check.log
/parser_errors.txt
/spec.rs.bak
/*.diff
/*.patch
/update.diff

# Root-level scratch files (all forms: .py, .vox, .rs, .json that aren't schema)
/scratch_*.py
/tmp_*.py
/annotate.py
/extract.py
/fix_links.py
/scratch_*.vox
/scratch_*.rs
/scratch_*.json
/scratch_*.vox-ir.json
/error_test.vox
/lychee_test.json

# Codex cutover sidecars (agent-generated, not VCS)
check.log
parser_errors.txt

# Generated files
crates/vox-orchestrator/src/generated/

**/tsconfig.tsbuildinfo
crates/vox-dashboard/dist/
docs/src/SUMMARY.md
docs/src/feed.xml
docs/src/architecture/architecture-index.md


# insta pending-review snapshots — local-only, regenerated each test run
**/*.snap.new
_bundle_ai_fixture_*

# Vox GUI dev run logs
gui-*.log

# Local `act` artifact server output (see .actrc --artifact-server-path)
.act-artifacts/

# Superpowers brainstorm visual-companion scratch (local-only)
.superpowers/

# Local `act` artifact server output (see .actrc --artifact-server-path)
.act-artifacts/
# graphify knowledge-graph artifacts (local tooling) — large, regenerable.
# NOTE: `graphify-out/*` (not `graphify-out/`) so the negations below can
# re-include specific committed files — git cannot un-ignore a path whose
# *parent directory* (as opposed to its contents) is excluded.
graphify-out/*

# Graphify generated behavior coverage files (Tier D — not versioned)
graphify-out/COVERAGE_BEHAVIORS_*

# Exception: scripts/db-table-census.vox + scripts/db-test-census.vox outputs
# ARE committed — they're the re-runnable recurrence-prevention census (see
# docs/src/architecture/2026-08-01-voxdb-audit-condensation-plan.md Task 1.3),
# meant to be periodically regenerated and re-committed, not local-only scratch.
!graphify-out/table_usage_report.json
!graphify-out/quarantine_test_findings.json

# Isolated git worktrees for parallel development
.worktrees/

# Antigravity agents custom rules and skills mount (Track D T0)
/.agents/

# Scratch output from `cargo ... > file 2>&1` redirects (Windows process-orphan
# avoidance). Never commit these — agents redirect cargo output here.
test-out.txt
build-out.txt
voxup-deps.txt
*-out.txt


# Per-machine mesh state: contains VOX_MESH_TOKEN. Never commit.
/.vox/populi/

# graphify pre-run backup: regenerable local artifact, same class as graphify-out/*
graphify-out.pre-graphify-backup/
