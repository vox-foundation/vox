[workspace]
resolver = "2"
members = ["crates/*","crates/workspace-hack"]
exclude = [
    "crates/_corpus_verify_tmp",
    "crates/vox-cargo-shim",
    "crates/vox-arch-check/tests/fixtures/missing-desc",

    "crates/vox-plugin-cloud",
    "crates/vox-plugin-host/tests/fixtures/noop-code",
    "crates/vox-plugin-host/tests/fixtures/noop-code-bad-abi",
    "crates/vox-plugin-host/tests/fixtures/noop-skill",
    "crates/vox-plugin-skill-compiler",
    "crates/vox-plugin-skill-git",
    "crates/vox-plugin-skill-memory",
    "crates/vox-plugin-skill-orchestrator",
    "crates/vox-plugin-skill-rag",
    "crates/vox-plugin-skill-testing",
    "crates/vox-plugin-skill-testing-validate",
    "crates/vox-plugin-skill-v0",
    # Telemetry ingest server — own self-contained workspace; ClickHouse deps
    # never enter the main cargo graph / arch-check. See server/telemetry/Cargo.toml.
    "server/telemetry",
]
default-members = ["crates/vox-cli"]

[workspace.package]
version = "0.6.0"
edition = "2024"
license = "Apache-2.0"
authors = ["Bert Brainerd"]
repository = "https://github.com/vox-foundation/vox.git"
rust-version = "1.96"
keywords = ["vox", "language", "compilers"]
categories = ["development-tools", "compilers"]

[workspace.lints.rust]
unsafe_code = "warn"
# `standalone` is vox-codegen-ts's own feature; the crate is also #[path]-embedded
# into vox-codegen (where the feature isn't set), so declare it known to silence the
# unexpected_cfgs warning in that embed context.
unexpected_cfgs = { level = "deny", check-cfg = ['cfg(feature, values("standalone"))'] }

[workspace.lints.clippy]
all = "warn"

[profile.dev]
incremental = true
split-debuginfo = "unpacked"
# Line tables only (vs full DWARF at debug=2). Backtraces and breakpoints still
# work. Cuts rlib size for first-party crates by ~70-80%; the 1.7 GB
# libvox_orchestrator rlib was the largest single contributor to the 86 GB
# target/ before this change.
debug = 1

[profile.dev.package."*"]
# Keep opt-level=1 for deps: parsing and serde noticeably faster at dev time.
# But strip their debug info — you almost never step into dep source in a
# debugger, and the savings compound across 40+ stale rlib copies in deps/.
opt-level = 1
debug = 0

[workspace.dependencies]
vox-scientia = { path = "crates/vox-scientia" }
vox-ast                   = { path = "crates/vox-ast" }
workspace-hack            = { path = "crates/workspace-hack" }
# Internal crates
vox-build-meta            = { path = "crates/vox-build-meta" }
vox-redact                = { path = "crates/vox-redact" }
vox-telemetry             = { path = "crates/vox-telemetry" }
vox-telemetry-otlp        = { path = "crates/vox-telemetry-otlp" }
vox-foundation            = { path = "crates/vox-foundation" }
vox-llm-config            = { path = "crates/vox-llm-config" }
vox-llm-egress            = { path = "crates/vox-llm-egress" }
vox-oauth-pkce            = { path = "crates/vox-oauth-pkce" }


vox-audit                 = { path = "crates/vox-audit" }
vox-capability-registry   = { path = "crates/vox-capability-registry" }
vox-secrets                = { path = "crates/vox-secrets", version = "0.6.0" }
vox-cli                   = { path = "crates/vox-cli" }
vox-bounded-fs            = { path = "crates/vox-bounded-fs", version = "0.6.0" }
vox-compiler              = { path = "crates/vox-compiler" }
vox-codegen         = { path = "crates/vox-codegen", default-features = false }
vox-codegen-ts      = { path = "crates/vox-codegen-ts" }
vox-rn-codegen      = { path = "crates/vox-rn-codegen" }
vox-config                = { path = "crates/vox-config" }
vox-config-derive         = { path = "crates/vox-config-derive" }
vox-graph-reader          = { path = "crates/vox-graph-reader" }
vox-container             = { path = "crates/vox-container" }
vox-container-types       = { path = "crates/vox-container-types" }
vox-deploy-codegen        = { path = "crates/vox-deploy-codegen" }
vox-skill-runtime         = { path = "crates/vox-skill-runtime" }


vox-jsonschema-util       = { path = "crates/vox-jsonschema-util" }
vox-corpus                = { path = "crates/vox-corpus" }
vox-db                    = { path = "crates/vox-db" }
vox-sql                   = { path = "crates/vox-sql" }
vox-db-types              = { path = "crates/vox-db-types" }
vox-eval                  = { path = "crates/vox-eval" }

vox-forge                 = { path = "crates/vox-forge" }
vox-crypto                = { path = "crates/vox-crypto", version = "0.6.0" }
vox-identity              = { path = "crates/vox-identity" }

vox-openclaw-runtime           = { path = "crates/vox-openclaw-runtime" }
vox-doc-inventory         = { path = "crates/vox-doc-inventory" }
vox-doc-pipeline          = { path = "crates/vox-doc-pipeline" }
vox-integration-tests     = { path = "crates/vox-integration-tests" }
vox-mcp-registry          = { path = "crates/vox-mcp-registry" }

vox-test-harness          = { path = "crates/vox-test-harness" }
vox-plugin-test-harness   = { path = "crates/vox-plugin-test-harness" }
vox-git                   = { path = "crates/vox-git" }
vox-vcs                   = { path = "crates/vox-vcs" }
vox-constrained-gen       = { path = "crates/vox-constrained-gen" }
vox-grammar-export        = { path = "crates/vox-grammar-export" }
vox-language-surface      = { path = "crates/vox-language-surface" }

vox-lsp                   = { path = "crates/vox-lsp" }
vox-gamify                 = { path = "crates/vox-gamify" }
vox-hf-layout              = { path = "crates/vox-hf-layout" }
vox-populi                = { path = "crates/vox-populi" }
vox-populi-types          = { path = "crates/vox-populi-types" }
vox-ml-cli                  = { path = "crates/vox-ml-cli" }
vox-mesh-policy           = { path = "crates/vox-mesh-policy" }
vox-mesh-transport        = { path = "crates/vox-mesh-transport", version = "0.6.0" }
vox-mesh-types            = { path = "crates/vox-mesh-types", version = "0.6.0" }

vox-effort-audit          = { path = "crates/vox-effort-audit" }
vox-effort-route          = { path = "crates/vox-effort-route" }
vox-speech                = { path = "crates/vox-speech" }
vox-orchestrator          = { path = "crates/vox-orchestrator" }
vox-research-shim              = { path = "crates/vox-research-shim" }

vox-orchestrator-test-helpers = { path = "crates/vox-orchestrator-test-helpers" }
vox-orchestrator-mcp      = { path = "crates/vox-orchestrator-mcp" }
vox-orchestrator-queue    = { path = "crates/vox-orchestrator-queue" }
vox-orchestrator-driver   = { path = "crates/vox-orchestrator-driver" }
vox-orchestrator-d        = { path = "crates/vox-orchestrator-d" }
vox-orchestrator-types    = { path = "crates/vox-orchestrator-types" }


vox-openai             = { path = "crates/vox-openai" }
vox-plugin-api            = { path = "crates/vox-plugin-api", version = "0.6.0" }
vox-plugin-sdk            = { path = "crates/vox-plugin-sdk", version = "0.6.0" }
vox-plugin-types          = { path = "crates/vox-plugin-types", version = "0.6.0" }
vox-plugin-catalog        = { path = "crates/vox-plugin-catalog" }
vox-plugin-host           = { path = "crates/vox-plugin-host" }
vox-plugin-nvml-probe     = { path = "crates/vox-plugin-nvml-probe" }
vox-plugin-runtime-container = { path = "crates/vox-plugin-runtime-container" }
vox-plugin-runtime-wasm   = { path = "crates/vox-plugin-runtime-wasm" }
vox-plugin-publication    = { path = "crates/vox-plugin-publication" }
vox-wasm-engine           = { path = "crates/vox-wasm-engine" }
vox-package                    = { path = "crates/vox-package" }
vox-package-types              = { path = "crates/vox-package-types" }



vox-publisher             = { path = "crates/vox-publisher", default-features = false }
vox-cli-core              = { path = "crates/vox-cli-core" }
vox-cli-ci                = { path = "crates/vox-cli-ci" }
vox-cli-share            = { path = "crates/vox-cli-share" }
vox-cli-research          = { path = "crates/vox-cli-research" }
vox-cli-review            = { path = "crates/vox-cli-review" }
vox-cli-contracts         = { path = "crates/vox-cli-contracts" }


vox-project-scaffold      = { path = "crates/vox-project-scaffold" }
vox-http-client        = { path = "crates/vox-http-client" }

vox-research-events       = { path = "crates/vox-research-events" }



vox-quantize             = { path = "crates/vox-quantize" }
vox-rename-registry       = { path = "crates/vox-rename-registry" }
vox-repository            = { path = "crates/vox-repository" }
vox-shell-stdlib-types    = { path = "crates/vox-shell-stdlib-types" }
vox-rule-pack             = { path = "crates/vox-rule-pack" }
vox-actor-runtime               = { path = "crates/vox-actor-runtime" }
vox-scaling-policy        = { path = "crates/vox-scaling-policy", version = "0.6.0" }

vox-skills                = { path = "crates/vox-skills" }

vox-tensor                = { path = "crates/vox-tensor" }
vox-code-audit               = { path = "crates/vox-code-audit" }
vox-drift-check              = { path = "crates/vox-drift-check" }
vox-workflow-runtime      = { path = "crates/vox-workflow-runtime" }
vox-search                = { path = "crates/vox-search" }
vox-similarity            = { path = "crates/vox-similarity" }
vox-skill-discovery       = { path = "crates/vox-skill-discovery" }
vox-tauri-codegen         = { path = "crates/vox-tauri-codegen" }
vox-tauri-stt             = { path = "crates/vox-tauri-stt" }




# External dependencies (centralized versions)
abi_stable         = "0.11"
logos              = "0.16"
clap               = { version = "4.5", features = ["derive"] }
clap_complete      = { version = "4.5" }
serde              = { version = "1", features = ["derive"] }
serde_json         = "1"
# serde_yaml 0.9 is archived/deprecated (RUSTSEC-flagged 0.9.34+deprecated). serde_yaml_ng
# is the API-compatible maintained fork; the package-rename keeps `serde_yaml::` paths working
# workspace-wide with zero source changes. (Do NOT use serde_yml — RUSTSEC-2025-0068, unsound.)
serde_yaml         = { package = "serde_yaml_ng", version = "0.10" }
toml               = "0.8"
toml_edit          = "0.22"
typify             = "0.7"
miette             = { version = "7.6", features = ["fancy"] }
aho-corasick       = "1"
nucleo             = { version = "0.5" }
ratatui            = { version = "0.30" }
crossterm          = { version = "0.29" }
reedline           = { version = "0.48" }
alacritty_terminal = { version = "0.26" }
portable-pty       = "0.9"
tokio              = { version = "1", features = ["rt", "macros", "sync", "time", "net", "process", "io-util", "fs", "rt-multi-thread", "signal"] }
tokio-stream       = { version = "0.1", default-features = false }
tracing            = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-test       = { version = "0.2", features = ["no-env-filter"] }
anyhow             = "1"
reqwest            = { version = "0.12", features = ["json", "rustls-tls", "stream"], default-features = false }
reqwest-middleware = { version = "0.4", default-features = false, features = ["json", "rustls-tls"] }
reqwest-retry      = { version = "0.7", default-features = false }
http               = "1"
bsky-sdk = { version = "0.1", default-features = false, features = ["atrium-xrpc-client", "rich-text", "default-client"] }
atrium-api = { version = "0.25", default-features = false, features = ["bluesky"] }
sha2               = "0.11"
sha3               = "0.12"
bincode            = "2"
data-encoding      = "2"
thiserror          = "2"
base64             = "0.22"
owo-colors         = "4"
chrono             = { version = "0.4", default-features = false, features = ["clock", "std", "serde"] }
regex              = "1"
regress            = "0.11"
comfy-table        = "7"
csv                = "1.3"
tempfile           = "3"
turso              = { version = "0.6", default-features = false, features = ["sync"] }
blake3             = { version = "1", features = ["pure"] }
jsonwebtoken       = { version = "10", default-features = false, features = ["rust_crypto"] }
ed25519-dalek      = { version = "2", default-features = false }
x25519-dalek       = { version = "2", default-features = false, features = ["static_secrets"] }
# Mesh transport (ADR-047). `default-features = false` deliberately drops iroh's
# `portmapper` default: it is what performs UPnP/PCP gateway probing and SSDP
# multicast, which raises a firewall dialog on macOS and buys nothing on a LAN
# mesh addressed by ticket. `fast-apple-datapath` is kept from the default set.
iroh               = { version = "1.1", default-features = false, features = ["tls-ring", "fast-apple-datapath"] }
iroh-mdns-address-lookup = "=0.5.0"   # pre-1.0: pinned exactly, per plan Task 1.3
iroh-tickets       = "1.0"   # 0.1 pins ed25519-dalek =3.0.0-pre.1 and cannot resolve with iroh 1.1
postcard           = { version = "1", default-features = false, features = ["use-std"] }
rustls             = { version = "0.23", default-features = false, features = ["ring"] }
rustls-pemfile     = "2"
tokio-rustls       = { version = "0.26", default-features = false, features = ["ring"] }
insta              = { version = "1", features = ["json"] }
criterion          = { version = "0.5", features = ["html_reports"] }
async-trait        = "0.1"
futures-util       = "0.3"
glob               = "0.3"
hound              = "3.5.1"
hex                = "0.4.3"
dirs               = "6"
mime_guess         = "2.0"
uuid               = { version = "1", features = ["v4", "serde"] }
async-stream       = "0.3"
xxhash-rust        = { version = "0.8", features = ["xxh3"] }
sysinfo            = "0.39"
walkdir            = "2"
petgraph           = "0.8.3"
leiden-rs          = "0.8.1"
tree-sitter        = "0.26.9"
tree-sitter-rust   = "0.24.2"
tree-sitter-typescript = "0.23.2"
tree-sitter-python = "0.23.6"
rayon              = "1.10"
rand               = { version = "0.8", features = ["small_rng", "std_rng"] }
candle-core         = { version = "0.10", default-features = false }
candle-nn           = { version = "0.10", default-features = false }
candle-transformers = { version = "0.10", default-features = false }
safetensors        = "0.7"
tokenizers         = { version = "0.21", default-features = false, features = ["fancy-regex"] }
memmap2            = "0.9"
# 0.4+ resolves Hugging Face relative `Location` redirects in tokio metadata (0.3 breaks with `relative URL without a base`).
# Narrowed to what vox actually uses: `api::sync::Api` (ureq) and
# `api::tokio::Api` (tokio). `default` additionally pulls `default-tls` ->
# `native-tls` -> `reqwest?/default`, which we do not use.
#
# This does NOT by itself remove native-tls or aws-lc-rs from the graph —
# `reqwest/default` is still enabled by other paths. See
# `contracts/crypto/transport-providers.v1.json` for the full provider
# accounting and why two providers are currently unavoidable.
hf-hub             = { version = "1", default-features = false, features = ["blocking", "rustls-tls"] }
keyring            = "3"
qlora-rs           = "1.0.5"
peft-rs            = "1.0"
bytes              = "1"
schemars           = { version = "1", features = ["derive"] }
jsonschema         = { version = "0.46", default-features = false }
openapiv3          = "2"
governor           = "0.10"
strum              = { version = "0.28", features = ["derive"] }
serial_test        = { version = "3", features = ["file_locks"] }
wiremock           = "0.6"
tantivy            = { version = "0.22", default-features = false }
scraper            = "0.20"
html2text          = "0.12"
# Optional NVIDIA management library bindings (runtime `nvml.dll` / `libnvidia-ml.so`).
nvml-wrapper       = { version = "0.12", default-features = false }
# Wave 5: Async mesh results
dashmap            = { version = "6", default-features = false }
rust_decimal       = { version = "1.36", features = ["serde", "db-diesel-postgres"], default-features = false }
aes-gcm            = "0.10"
unicode-segmentation = "1.12"
argon2             = "0.5"

# [VOX AUDIT 2026-04-13] Centralized sprawling versions found in sub-crates
axum               = { version = "0.8", default-features = false }
axum-extra         = "0.12.5"
hyper              = { version = "1", features = ["full"] }
hyper-util         = { version = "0.1", features = ["client", "client-legacy", "tokio"] }
tower-http         = "0.6.2"
subtle             = "2.6"
futures            = "0.3"
chacha20poly1305   = "0.10"
aead               = { version = "0.5", features = ["std"] }
gix                = { version = "0.84", default-features = false }
jj-lib             = { version = "0.42", default-features = false }
wasmtime           = "45.0.2"
wasmtime-wasi      = "45.0.2"

self_update        = "0.44"
landlock           = "0.4.4"
win32job           = "2"
windows-sys        = "0.59"
tavily             = "2.1.0"
quick-xml          = "0.41"
flate2             = { version = "1", default-features = false, features = ["rust_backend"] }
bzip2              = "0.6.1"
zstd               = "0.13.3"
hmac               = "0.13"
crossbeam-queue    = "0.3.12"
ignore             = "0.4.25"
tauri              = { version = "2", features = [] }
tokio-tungstenite  = { version = "0.24", default-features = false }
rubato             = "0.15.0"
symphonia          = { version = "0.5.4", default-features = false }
sherpa-onnx        = "1.13"
syn                = "2"
prettyplease       = "0.2"
proc-macro2        = "1"
semver             = "1"
cargo_metadata     = "0.23"
parking_lot        = "0.12"
secrecy            = "0.10"
zeroize            = { version = "1", features = ["derive"] }
# wiremock 0.6.5 pulls in deadpool 0.12.3 with no runtime backend feature.
# Under rustc 1.92, `Runtime::timeout`'s body cfg-strips both arms and ends
# up returning `!`, which then breaks type inference inside
# `deadpool::unmanaged::Pool::timeout_get` even though that code path is
# never instantiated by wiremock. Forcing `rt_tokio_1` here gives the match
# a concrete arm so inference succeeds. Tracked upstream — remove once
# wiremock activates a runtime explicitly.
deadpool           = { version = "0.12", features = ["rt_tokio_1"] }
rmcp               = "1.7"
opentelemetry      = "0.29"
opentelemetry-otlp = { version = "0.29", default-features = false, features = ["http-proto", "reqwest-client"] }
opentelemetry_sdk  = { version = "0.29", features = ["rt-tokio"] }
tracing-opentelemetry = "0.30"
rmp-serde          = "1"
libloading         = "0.8"
lru                = "0.16"
futures-core       = "0.3"
quote              = "1"
similar            = "2"
strsim             = "0.11"
globset            = "0.4"
swc_ecma_parser    = "41"
swc_ecma_ast       = "25"
swc_ecma_visit     = "25"
swc_common         = "23"
which              = "8"
ctrlc              = "3.4"
proptest           = { version = "1", default-features = false, features = ["std"] }
# File watching
notify             = "8"
# Audio
cpal               = "0.15"
# Binary / stream utilities
byteorder          = "1"
tar                = "0.4"
zip                = "2.1"
libc               = "0.2"
# CLI UI
dialoguer          = "0.11"
pulldown-cmark     = "0.13"
url                = "2"
urlencoding        = "2"
# Language model / tokenization
tiktoken-rs        = { version = "0.12", default-features = false }
# Tower / LSP
tower              = { version = "0.5", default-features = false }
tower-lsp-server   = "0.23"
# Serde schemas (v0.8 alias used by some crates)
schemars08         = { package = "schemars", version = "0.8" }
# Rand extras
rand_distr         = "0.4"
rand09             = { package = "rand", version = "0.9", features = ["small_rng"] }
# Nanopublications: build, RSA-sign, and offline-validate signed RDF/TriG nanopubs (MIT licensed)
nanopub            = "0.2"


[patch.crates-io]
# Vox: stabilize deep o_proj proxy stacks + correct grad-accum scaling in `training_step_lm`.
qlora-rs = { path = "patches/qlora-rs-1.0.5" }
# Vox: pin peft-rs to candle-core 0.10 so it shares the same candle instance as qlora-rs and our crates.
peft-rs = { path = "patches/peft-rs-1.0.3" }
# Vox: enable pure-rust crypto on all targets so MSVC builds do not require clang-cl (aegis C backend).
aegis = { path = "patches/aegis-0.9.8" }
# Vox: fix rank>4 strided reduce indexing on Metal (broke GQA K/V grads in QLoRA training).
candle-metal-kernels = { path = "patches/candle-metal-kernels-0.10.2" }

[profile.release]
lto = "thin"
strip = "debuginfo"

[profile.ci]
inherits = "release"
lto = "thin"
strip = "debuginfo"

# Distribution profile: fat LTO + single codegen unit + symbol stripping.
# Produces the smallest, fastest binaries for shipping.
# Use: cargo build --profile dist -p vox-cli
# (Keep --release for fast local builds; --profile dist is for CI ship artifacts.)
#
# Deliberately does NOT set `panic = "abort"`. The workspace relies on unwinding
# in three non-test paths: vox-actor-runtime's spawn_supervised
# (JoinError::is_panic), vox-vcs's `guarded!` macro (catch_unwind around jj-lib,
# which ships via vox-orchestrator's default features), and vox-search's
# memory_cache (resume_unwind). Abort would turn each containment point into a
# process kill.
#
# `strip = "symbols"` does cost symbolicated backtraces on Linux/macOS (release
# strips only debuginfo). Nothing in the workspace captures a Backtrace, sets a
# panic hook, or asserts on backtrace content, so the only loss is
# RUST_BACKTRACE=1 quality in a user-reported panic. Switch to
# `strip = "debuginfo"` if that becomes a support burden.
[profile.dist]
inherits = "release"
lto = "fat"
codegen-units = 1
strip = "symbols"




