{
  "x-vox-version": 1,
  "version": 1,
  "suppressions": [
    {
      "rule_id_prefix": "skeleton/untested-pub-api",
      "path_glob": "**/vox-cli/src/commands/pm_lifecycle.rs",
      "reason": "File's 4 pub fns (open_local_pm_store, resolve_lockfile_from_manifest_local, lockfile_path, package_blob_dir) predate the 2026-09-21 fix for the vox-pm-registry-404 bug, which only reworded the strict-resolution error message. Surfaced only because tdd-guard re-scans the whole touched file, not the diff; unit-testing the full local-index resolution surface is a separate, out-of-scope effort.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "arch/god_object",
      "path_glob": "**/vox-secrets/src/spec.rs",
      "reason": "Legacy payload pending domain split",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "arch/god_object",
      "path_glob": "**/vox-compiler/tests/web_ir_lower_emit.rs",
      "reason": "Large test file",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "arch/god_object",
      "path_glob": "**/vox-populi/tests/http_control_plane.rs",
      "reason": "Large test file",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "arch/god_object",
      "path_glob": "**/vox-workflow-runtime/tests/workflow_tracker_tests.rs",
      "reason": "Large test file",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "arch/god_object",
      "path_glob": "**/vox-compiler/tests/reactive_smoke.rs",
      "reason": "Large test file",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "arch/god_object",
      "path_glob": "**/vox-cli/src/commands/harness/eval.rs",
      "reason": "Legacy: already 617 non-blank lines on main before the 2026-08-01 llm-guidance-token-audit merge touched it (added a Mutex-type + #[allow(unsafe_code)] fix, not new domain logic). Pending decomposition, not introduced by this change.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "arch/god_object",
      "path_glob": "**/vox-gui/src/commands/daemon.rs",
      "reason": "Legacy: already 525 non-blank lines on main before the 2026-08-01 llm-guidance-token-audit merge touched it (a clippy collapsible_if fix reduced it by 1 line). Pending decomposition, not introduced by this change.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/untested-pub-api",
      "path_glob": "**/vox-ml-cli/src/commands/mens/populi/dispatch.rs",
      "reason": "pub fn run() is the pre-existing CLI dispatch match (predates the 2026-09-10 qwen38-27b-hub Task 18 fix, which only edits the Serve arm's execution-api-off fallback). Unit-testing the full dispatch surface (constructing every PopuliAction variant, feature-gated arms) is a separate, out-of-scope effort.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/hollow-fn",
      "path_glob": "**/vox-plugin-browser/src/hosts.rs",
      "reason": "existing_host_policy's Ok(()) body is the pre-existing, intentional no-op policy (a host already in the map may take another page, so there is nothing to reject) -- predates the 2026-09-10 clippy-wave fix in this file, which only gates debug_host_key behind #[cfg(test)]. Surfaced only because tdd-guard re-scans the whole touched file, not the diff.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/untested-pub-api",
      "path_glob": "**/vox-plugin-mens-candle-metal/src/candle_qlora_train/training_loop/mod.rs",
      "reason": "run_training_loop drives a live Candle/Metal QLoRA trainer end to end; exercised via integration/manual training runs, not a unit harness. Pre-existing (file had zero #[test] blocks before Task 7 OOM wiring touched it).",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/untested-pub-api",
      "path_glob": "**/vox-plugin-mens-candle-cuda/src/candle_qlora_train/training_loop/mod.rs",
      "reason": "run_training_loop drives a live Candle/CUDA QLoRA trainer end to end; exercised via integration/manual training runs, not a unit harness. Pre-existing (file had zero #[test] blocks before Task 7 OOM wiring touched it).",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/untested-pub-api",
      "path_glob": "**/vox-ml-cli/src/commands/mens/eval_gate/check_run.rs",
      "reason": "check_run is pre-existing (predates Task A3's pass_at_k missing-baseline hard-fail fix, which only edits one else-branch) and is exercised by 69 #[test]s in the sibling eval_gate/tests.rs, including the new pass_at_k_gate_hard_fails_when_baseline_configured_but_missing added for this change. tdd-guard only looks for inline #[test] blocks in the same file, not the crate's established sibling-file test convention for this module.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/untested-pub-api",
      "path_glob": "**/vox-plugin-mens-candle-core/src/**",
      "reason": "Task E2: this crate's files are verbatim moves of code that was byte-for-byte duplicated between vox-plugin-mens-candle-metal and vox-plugin-mens-candle-cuda (or, for config.rs/manifest.rs/train_log.rs, a merge of two near-duplicate divergent copies). Every one of these functions had zero #[test] blocks in its original per-plugin location before the move (verified via `git show HEAD:<old-path> | grep -c '#\\[test\\]'` == 0 for each flagged file) -- tdd-guard flags them only because the file path is new, not because this task authored untested logic. Adding tests for this pre-existing, previously-untested code is a separate, out-of-scope effort from the fold this task performs.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/untested-pub-api",
      "path_glob": "**/vox-plugin-mens-candle-cuda/src/candle_qlora_train/training_loop/logic.rs",
      "reason": "Task E2: checkpointed_backward_step is pre-existing (predates this task, which only removed the file's other function, trajectory_weight_for_pair, in favor of a re-export from the new vox-plugin-mens-candle-core crate). The file had zero #[test] blocks before this task touched it; tdd-guard re-scans the whole file on any edit, not just the diff.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/hollow-fn",
      "path_glob": "**/vox-plugin-mens-candle-cuda/src/device.rs",
      "reason": "Task E2: trim_default_pool is pre-existing CUDA driver FFI, untouched by this task (which only replaced this file's DeviceKind/GpuInfo definitions with a re-export from vox-plugin-mens-candle-core). tdd-guard re-scans the whole touched file, not the diff.",
      "owner": "robot"
    },
    {
      "rule_id_prefix": "skeleton/hollow-fn",
      "path_glob": "**/vox-research-shim/src/research/provider.rs",
      "reason": "map_site is a pre-existing stub returning None (site crawling is handled via query.site_scope filtering; no dedicated site-map API exists in vox-search yet). Surfaced only because tdd-guard re-scans the whole touched file, not the diff.",
      "owner": "robot"
    }
  ]
}
