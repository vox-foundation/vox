# Bundle contract aliases

The canonical JSON Schema for the `Vox.toml` **`[bundle]`** table lives at:

[`contracts/manifest/vox-bundle.v1.schema.json`](../manifest/vox-bundle.v1.schema.json)

This directory exists so older references to `contracts/bundle/vox-toml-bundle.v1.schema.json` can redirect here without duplicating schema payloads.

See also: [`docs/src/architecture/vox-application-packaging-ssot-2026.md`](../../docs/src/architecture/vox-application-packaging-ssot-2026.md).
