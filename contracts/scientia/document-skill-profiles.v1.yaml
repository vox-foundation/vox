version: v1
skills:
  publication_evidence_authoring:
    category: documentation
    purpose: Structured claim-evidence authoring that maps directly to preflight and worthiness fields.
    required_outputs:
      - claims
      - evidence_links
      - seed_and_variance_notes
      - ai_disclosure_block
      - limitations
    ingest_policy:
      schema_validation_required: true
      bypass_hard_gates: false
  venue_policy_adaptation:
    category: documentation
    purpose: Transform canonical metadata into venue-ready policy packets without reauthoring core metadata.
    required_outputs:
      - route_profile
      - policy_delta
      - required_field_checklist
      - unresolved_items
    ingest_policy:
      schema_validation_required: true
      bypass_hard_gates: false
