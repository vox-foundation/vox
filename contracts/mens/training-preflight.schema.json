{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vox-lang.org/contracts/mens/training-preflight.schema.json",
  "title": "Mens training preflight record",
  "description": "F04: deterministic machine-readable preflight output — extend alongside FineTuneContract / planner fields. Phase 1 tightened: core keys required; optional fields remain open (`additionalProperties`).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "schema_version": { "type": "string", "const": "vox.mens.preflight.v0" },
    "contract_digest": { "type": "string", "minLength": 1 },
    "execution_kernel": { "type": "string", "minLength": 1 },
    "notes": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["schema_version", "contract_digest", "execution_kernel"]
}
