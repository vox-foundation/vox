        let mut mock_catalog = OperationsCatalog {
            schema_version: 1,
            capability: Default::default(),
            operations: vec![],
            exemptions: Default::default(),
        };
