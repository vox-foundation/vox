# 13 — hardcoded extensions globs

**Severity**: info  
**Itemized**: 61

### hv-0971 — `contracts/config/env-vars.v1.yaml:375`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/config/env-vars.v1.yaml"`

**Confidence**: medium

---

### hv-0972 — `contracts/documentation/canonical-map.v1.yaml:17`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0973 — `contracts/documentation/canonical-map.v1.yaml:18`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0974 — `contracts/documentation/canonical-map.v1.yaml:36`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0975 — `contracts/documentation/canonical-map.v1.yaml:53`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0976 — `contracts/documentation/canonical-map.v1.yaml:54`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0977 — `contracts/documentation/canonical-map.v1.yaml:55`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0978 — `contracts/documentation/canonical-map.v1.yaml:68`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0979 — `contracts/documentation/canonical-map.v1.yaml:69`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0980 — `contracts/documentation/canonical-map.v1.yaml:88`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0981 — `contracts/documentation/canonical-map.v1.yaml:89`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0982 — `contracts/documentation/canonical-map.v1.yaml:105`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0983 — `contracts/documentation/canonical-map.v1.yaml:106`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0984 — `contracts/documentation/canonical-map.v1.yaml:218`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0985 — `contracts/documentation/canonical-map.v1.yaml:219`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0986 — `contracts/documentation/canonical-map.v1.yaml:229`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0987 — `contracts/documentation/canonical-map.v1.yaml:239`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0988 — `contracts/documentation/canonical-map.v1.yaml:240`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/documentation/canonical-map.v1.yaml"`

**Confidence**: medium

---

### hv-0989 — `contracts/operations/completion-policy.v1.yaml:47`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/operations/completion-policy.v1.yaml"`

**Confidence**: medium

---

### hv-0990 — `contracts/reports/docs-reality-audit/inventory.v1.json:42`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/docs-reality-audit/inventory.v1.json"`

**Confidence**: medium

---

### hv-0991 — `contracts/reports/docs-reality-audit/inventory.v1.json:43`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/docs-reality-audit/inventory.v1.json"`

**Confidence**: medium

---

### hv-0992 — `contracts/reports/docs-reality-audit/inventory.v1.json:58`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/docs-reality-audit/inventory.v1.json"`

**Confidence**: medium

---

### hv-0993 — `contracts/reports/docs-reality-audit/inventory.v1.json:74`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/docs-reality-audit/inventory.v1.json"`

**Confidence**: medium

---

### hv-0994 — `contracts/reports/docs-reality-audit/inventory.v1.json:85`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/docs-reality-audit/inventory.v1.json"`

**Confidence**: medium

---

### hv-0995 — `contracts/reports/docs-reality-audit/inventory.v1.json:121`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/docs-reality-audit/inventory.v1.json"`

**Confidence**: medium

---

### hv-0996 — `contracts/reports/scaling-audit/findings-latest.json:10345`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/scaling-audit/findings-latest.json"`

**Confidence**: medium

---

### hv-0997 — `contracts/reports/sql-write-ownership-rev-c.json:6`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "contracts/reports/sql-write-ownership-rev-c.json"`

**Confidence**: medium

---

### hv-0998 — `crates/vox-arch-check/src/forbidden_patterns.rs:110`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "crates/vox-arch-check/src/forbidden_patterns.rs"`

**Confidence**: medium

---

### hv-0999 — `crates/vox-cli/src/commands/ci/db_schema_coverage.rs:77`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-cli/src/commands/ci/db_schema_coverage.rs"`

**Confidence**: medium

---

### hv-1000 — `crates/vox-cli/src/commands/ci/db_schema_coverage.rs:98`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-cli/src/commands/ci/db_schema_coverage.rs"`

**Confidence**: medium

---

### hv-1001 — `crates/vox-cli/src/commands/ci/pre_push.rs:503`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-cli/src/commands/ci/pre_push.rs"`

**Confidence**: medium

---

### hv-1002 — `crates/vox-cli/src/commands/ci/retired_symbol_check.rs:348`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "crates/vox-cli/src/commands/ci/retired_symbol_check.rs"`

**Confidence**: medium

---

### hv-1003 — `crates/vox-cli/src/commands/ci/retired_symbol_check.rs:377`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "crates/vox-cli/src/commands/ci/retired_symbol_check.rs"`

**Confidence**: medium

---

### hv-1004 — `crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs:59`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs"`

**Confidence**: medium

---

### hv-1005 — `crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs:128`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs"`

**Confidence**: medium

---

### hv-1006 — `crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs:669`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs"`

**Confidence**: medium

---

### hv-1007 — `crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs:687`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-cli/src/commands/ci/run_body_helpers/guards.rs"`

**Confidence**: medium

---

### hv-1008 — `crates/vox-cli/src/commands/ci/test_inventory.rs:152`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-cli/src/commands/ci/test_inventory.rs"`

**Confidence**: medium

---

### hv-1009 — `crates/vox-cli/src/commands/ci/test_inventory.rs:864`

**Substring**

```text
*.rs
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "*.rs" "crates/vox-cli/src/commands/ci/test_inventory.rs"`

**Confidence**: medium

---

### hv-1010 — `crates/vox-cli/src/commands/diagnostics/doctor/checks_codex.rs:167`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-cli/src/commands/diagnostics/doctor/checks_codex.rs"`

**Confidence**: medium

---

### hv-1011 — `crates/vox-cli/src/commands/diagnostics/stub_check/mod.rs:345`

**Substring**

```text
".json"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".json\"" "crates/vox-cli/src/commands/diagnostics/stub_check/mod.rs"`

**Confidence**: medium

---

### hv-1012 — `crates/vox-cli/src/commands/review/coderabbit/semantic_planner/groups.rs:38`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-cli/src/commands/review/coderabbit/semantic_planner/groups.rs"`

**Confidence**: medium

---

### hv-1013 — `crates/vox-cli/src/commands/review/coderabbit/semantic_planner/types.rs:209`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-cli/src/commands/review/coderabbit/semantic_planner/types.rs"`

**Confidence**: medium

---

### hv-1014 — `crates/vox-cli/src/commands/review/coderabbit/stack_planner/heuristics.rs:69`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-cli/src/commands/review/coderabbit/stack_planner/heuristics.rs"`

**Confidence**: medium

---

### hv-1015 — `crates/vox-cli/src/commands/review/coderabbit/stack_planner/heuristics.rs:190`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-cli/src/commands/review/coderabbit/stack_planner/heuristics.rs"`

**Confidence**: medium

---

### hv-1016 — `crates/vox-cli/src/v0.rs:26`

**Substring**

```text
".tsx"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".tsx\"" "crates/vox-cli/src/v0.rs"`

**Confidence**: medium

---

### hv-1017 — `crates/vox-corpus/src/corpus/mix/mod.rs:368`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-corpus/src/corpus/mix/mod.rs"`

**Confidence**: medium

---

### hv-1018 — `crates/vox-db/src/retrieval.rs:200`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-db/src/retrieval.rs"`

**Confidence**: medium

---

### hv-1019 — `crates/vox-doc-inventory/src/file_entry.rs:21`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-doc-inventory/src/file_entry.rs"`

**Confidence**: medium

---

### hv-1020 — `crates/vox-doc-inventory/src/file_entry.rs:23`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-doc-inventory/src/file_entry.rs"`

**Confidence**: medium

---

### hv-1021 — `crates/vox-doc-inventory/src/file_entry.rs:25`

**Substring**

```text
".ts"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".ts\"" "crates/vox-doc-inventory/src/file_entry.rs"`

**Confidence**: medium

---

### hv-1022 — `crates/vox-orchestrator/src/observer.rs:351`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-orchestrator/src/observer.rs"`

**Confidence**: medium

---

### hv-1023 — `crates/vox-orchestrator/src/planning/plan_adequacy.rs:330`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-orchestrator/src/planning/plan_adequacy.rs"`

**Confidence**: medium

---

### hv-1024 — `crates/vox-orchestrator/src/planning/synthesizer.rs:25`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-orchestrator/src/planning/synthesizer.rs"`

**Confidence**: medium

---

### hv-1025 — `crates/vox-publisher/src/scholarly/zenodo.rs:294`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-publisher/src/scholarly/zenodo.rs"`

**Confidence**: medium

---

### hv-1026 — `crates/vox-publisher/src/scholarly/zenodo.rs:297`

**Substring**

```text
".json"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".json\"" "crates/vox-publisher/src/scholarly/zenodo.rs"`

**Confidence**: medium

---

### hv-1027 — `crates/vox-publisher/src/submission/validation.rs:188`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-publisher/src/submission/validation.rs"`

**Confidence**: medium

---

### hv-1028 — `crates/vox-publisher/src/submission/validation.rs:203`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-publisher/src/submission/validation.rs"`

**Confidence**: medium

---

### hv-1029 — `crates/vox-publisher/src/submission/validation.rs:205`

**Substring**

```text
".json"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".json\"" "crates/vox-publisher/src/submission/validation.rs"`

**Confidence**: medium

---

### hv-1030 — `crates/vox-repository/src/workspace_path_migration.rs:87`

**Substring**

```text
".md"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".md\"" "crates/vox-repository/src/workspace_path_migration.rs"`

**Confidence**: medium

---

### hv-1031 — `crates/vox-search/src/execution.rs:95`

**Substring**

```text
".rs"
```

**Why it matters**: Extension lists diverge between tools; centralize to avoid scan gaps.

**Fix** (centralize-in-contract): Centralize allowed extensions in one MODULE or contract list consumed by scanners.

**SSOT**: `contracts/code-audit/rules.v1.yaml`

**Verify**: `rg -nF "\".rs\"" "crates/vox-search/src/execution.rs"`

**Confidence**: medium

---

