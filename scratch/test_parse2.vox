fn main() {
    let args = env.args()
    let script_idx = 0
    let i = 0
    while i < args.len() {
        let arg_str = args[i]
        if arg_str.contains("vox-entrypoint.vox") {
            script_idx = i
        }
        i = i + 1
    }
    print("Script index:", script_idx)
}
