// vox:caps process
import process;

fn main() {
    let res = process.run("cmd", ["/c", "echo hello"]);
    if res is null {
        print("process.run(cmd) failed");
    } else {
        print("Result: " + res.stdout.trim());
    }
}
