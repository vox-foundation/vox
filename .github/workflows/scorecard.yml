# OpenSSF Scorecard — read-only supply-chain posture signal (weekly).
# Results appear on the repo Security tab when publish_results is enabled.
name: OpenSSF Scorecard

on:
  schedule:
    # Weekly, Monday 06:29 UTC
    - cron: "29 6 * * 1"
  workflow_dispatch:

permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # OIDC token is required for publish_results: true (the action signs the
    # result before publishing). SARIF is only uploaded as an artifact here,
    # so security-events: write is deliberately NOT granted.
    permissions:
      id-token: write
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Run OpenSSF Scorecard
        uses: ossf/scorecard-action@v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload SARIF artifact
        uses: actions/upload-artifact@v7
        with:
          name: scorecard-sarif
          path: results.sarif
          if-no-files-found: error
