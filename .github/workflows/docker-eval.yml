name: Build & Push vox-eval Docker Image

on:
  # Not on push:main: build-and-push's 180m timeout (image build + registry
  # push) exceeds the fast-lane (branch push) 30-min cap (workflow_policy_guard).
  # Already ran daily in addition to push; now schedule-only.
  schedule:
    # Daily 03:00 UTC to keep the image fresh without triggering on every Rust commit
    - cron: "0 3 * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  REGISTRY: ghcr.io
  # Published under the personal namespace, not vox-foundation: the org's
  # free-tier plan blocks changing container-package visibility, and this
  # image carries no secrets/workspace by design, so it must be pullable
  # without registry credentials from the public Coolify deploy host.
  # SSOT: ghcr.io/brbrainerd/vox-eval (flipped to Public 2026-07-18 via the
  # package's own Danger Zone settings, personal account, no org linkage).
  IMAGE_NAME: brbrainerd/vox-eval

jobs:
  build-and-push:
    name: Build vox-eval image
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        # GITHUB_TOKEN can only publish to packages under the repo's own
        # owner (vox-foundation/*); pushing to a personal namespace
        # (brbrainerd/vox-eval) needs a PAT scoped to that account.
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: brbrainerd
          password: ${{ secrets.GHCR_PAT }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=main-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          # Deliberately NOT passing steps.meta.outputs.labels: the generated
          # org.opencontainers.image.source label points at vox-foundation/vox,
          # and a repo-source label can link a GHCR package to that repo.
          # Suppressed defensively so this personal-namespace package stays
          # under the personal account's own visibility control.
          labels: |
            org.opencontainers.image.description=Vox eval sandbox (vox mcp HTTP gateway; no workspace, no keys)
          # Cache layers between builds using the registry
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-to: type=inline
          build-args: |
            VOX_CLI_FEATURES=mcp-server

      # Report-only container vulnerability scan of the image we just pushed.
      # exit-code: '0' means findings are surfaced in the job log but never fail
      # the build. Once the image is clean, tighten this to exit-code: '1' to
      # make CRITICAL/HIGH vulnerabilities merge-blocking.
      - name: Scan image for vulnerabilities (Trivy, report-only)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          format: 'table'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
          exit-code: '0'
          # Don't persist the trivy vuln DB in the Actions cache. Its default
          # daily-rotating `cache-trivy-<date>` keys accumulate ~7 x 85 MB in a
          # 10 GB LRU bucket that the Rust caches actually need; one DB download
          # per daily run is the cheaper trade.
          cache: 'false'
