name: vox-mental-tracker

on:
  push:
    paths:
      - "apps/vox-mental-tracker/**"
      - ".github/workflows/vox-mental-tracker.yml"
  pull_request:
    paths:
      - "apps/vox-mental-tracker/**"
      - ".github/workflows/vox-mental-tracker.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: apps/vox-mental-tracker

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  vox-check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-rust
      - name: Install system deps (dbus/GTK for wry/keyring/soup on Linux)
        run: sudo apt-get update -y && sudo apt-get install -y libdbus-1-dev pkg-config libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev
      - name: Cargo smoke (parent workspace)
        run: cargo run -q -p vox-cli -- check apps/vox-mental-tracker/src/main.vox
        working-directory: "."

  vitest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - name: Install system deps (dbus/GTK for wry/keyring/soup on Linux)
        run: sudo apt-get update -y && sudo apt-get install -y libdbus-1-dev pkg-config libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev
      - uses: pnpm/action-setup@v6
        with:
          version: 11
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: "pnpm"
          cache-dependency-path: apps/vox-mental-tracker/pnpm-lock.yaml
      - name: Install JS deps
        run: pnpm install --frozen-lockfile
        working-directory: apps/vox-mental-tracker
      - name: Build Vox app (populate dist/)
        run: pnpm build:vox
        working-directory: apps/vox-mental-tracker
      - name: Vitest
        run: pnpm exec vitest run
        working-directory: apps/vox-mental-tracker

  playwright:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Install system deps (dbus/GTK for wry/keyring/soup on Linux)
        run: sudo apt-get update -y && sudo apt-get install -y libdbus-1-dev pkg-config libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev
      - uses: pnpm/action-setup@v6
        with:
          version: 11
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: "pnpm"
          cache-dependency-path: apps/vox-mental-tracker/pnpm-lock.yaml
      - name: Install JS deps
        run: pnpm install --frozen-lockfile
        working-directory: apps/vox-mental-tracker
      - name: Resolve Playwright version
        id: pw_version
        shell: bash
        run: |
          ver=$(node -p "require('./apps/vox-mental-tracker/node_modules/@playwright/test/package.json').version")
          echo "ver=$ver" >> "$GITHUB_OUTPUT"
        working-directory: "."
      # Only `main` WRITES: this workflow runs on every branch push and every
      # PR, and a PR-scoped cache entry is unreadable from any other ref while
      # still evicting main-scope entries out of the 10 GB LRU bucket.
      # Machine-checked by `vox ci cache-key-lint`.
      - name: Cache Playwright browsers (main saves)
        id: pw_cache_save
        if: github.ref == 'refs/heads/main'
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-${{ runner.os }}-${{ steps.pw_version.outputs.ver }}-chromium
          restore-keys: |
            ms-playwright-${{ runner.os }}-${{ steps.pw_version.outputs.ver }}-
            ms-playwright-${{ runner.os }}-
      - name: Restore Playwright browsers (no save off main)
        id: pw_cache_restore
        if: github.ref != 'refs/heads/main'
        uses: actions/cache/restore@v5
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-${{ runner.os }}-${{ steps.pw_version.outputs.ver }}-chromium
          restore-keys: |
            ms-playwright-${{ runner.os }}-${{ steps.pw_version.outputs.ver }}-
            ms-playwright-${{ runner.os }}-
      - name: Install Playwright browsers (cache miss)
        # Exactly one of the two steps above ran; the other's outputs are empty,
        # so this is a miss unless whichever ran reported a hit.
        if: steps.pw_cache_save.outputs.cache-hit != 'true' && steps.pw_cache_restore.outputs.cache-hit != 'true'
        run: pnpm exec playwright install chromium
        working-directory: apps/vox-mental-tracker
      - name: Playwright
        run: pnpm exec playwright test
        working-directory: apps/vox-mental-tracker
      - name: Upload route screenshots
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: vox-mental-tracker-screens
          path: apps/vox-mental-tracker/tests/e2e/__screens__/
          if-no-files-found: ignore

  contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-rust
      - name: Install system deps (dbus/GTK for wry/keyring/soup on Linux)
        run: sudo apt-get update -y && sudo apt-get install -y libdbus-1-dev pkg-config libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev
      - name: Build vox-cli
        run: cargo build -p vox-cli
      - name: JSON parse check on event-payload contracts
        run: cargo run -p vox-cli --quiet ci json-parse-check 'apps/vox-mental-tracker/contracts/event-payloads/*.json'
      - name: YAML parse check on export contracts
        run: cargo run -p vox-cli --quiet ci yaml-parse-check 'apps/vox-mental-tracker/contracts/export/*.yaml'

  app-summary:
    needs: [vox-check, vitest, playwright, contracts]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: always()
    steps:
      - name: Aggregate
        run: |
          if [ "${{ needs.vox-check.result }}" != "success" ] \
             || [ "${{ needs.vitest.result }}" != "success" ] \
             || [ "${{ needs.playwright.result }}" != "success" ] \
             || [ "${{ needs.contracts.result }}" != "success" ]; then
            echo "One or more lanes failed:"
            echo "  vox-check=${{ needs.vox-check.result }}"
            echo "  vitest=${{ needs.vitest.result }}"
            echo "  playwright=${{ needs.playwright.result }}"
            echo "  contracts=${{ needs.contracts.result }}"
            exit 1
          fi
          echo "All lanes green."
